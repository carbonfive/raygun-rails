test:
  override:
    - bundle exec rake
  post:
    - bundle exec rake db:sample_data

deployment:
  acceptance:
    branch: development
    commands:
      - ./bin/deploy.sh app-prototype-acceptance:
          timeout: 360

  production:
    branch: master 
    commands:
      - ./bin/deploy.sh app-prototype:
          timeout: 360
